const Header = (props) => {
  return (
    <header className="App-header">
      <p>Calculator</p>
      
        <button>-</button>
        <button>+</button>
        <button>X</button>
      
    </header>
  );
};

export default Header;
